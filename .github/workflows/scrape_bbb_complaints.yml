name: Scrape BBB Complaints

on:
  schedule:
    - cron: "0 8 * * 1"   # Every Monday at 8:00 AM UTC
  workflow_dispatch:       # Also allows you to run it manually from GitHub

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run scraper
        env:
          WEBSCRAPING_AI_KEY_COMPLAINTS: ${{ secrets.WEBSCRAPING_AI_KEY_COMPLAINTS }}
        run: python scrape_bbb_complaints.py

      - name: Commit and push results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add bbb_complaints.json
          git diff --cached --quiet || git commit -m "Update BBB complaints data [$(date -u '+%Y-%m-%d %H:%M UTC')]"
          git push
